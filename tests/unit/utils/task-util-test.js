import taskUtil from 'task-manager/utils/task-util';
import { module, test } from 'qunit';

module('Unit | Utility | task util');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = taskUtil();
  assert.ok(result);
});
